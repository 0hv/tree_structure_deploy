# tree_structure_deploy
This Python project analyzes a directory structure's textual representation, locates file and directories positions, saves paths, and uses them to recreate the original structure using graphical character descriptions.
